{
  "name": "eval-guestbook-web",
  "version": "1.0.0",
  "private": true,
  "description": "Guestbook frontend: serves the page and proxies /api/* to the backend",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20 <23"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
