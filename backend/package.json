{
  "name": "eval-guestbook-api",
  "version": "1.0.0",
  "private": true,
  "description": "Guestbook backend JSON API (Express + Postgres)",
  "scripts": {
    "start": "node server.js",
    "migrate": "node migrate.js"
  },
  "engines": {
    "node": ">=20 <23"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
